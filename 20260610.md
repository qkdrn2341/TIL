
## 20260610 수업일지

## 오늘 배운것
EntityManager는 영속성 컨텍스트를 통해 엔티티를 관리하며, persist(), find(), remove() 등의 작업은 먼저 영속성 컨텍스트에 반영된다. 이후 flush() 시 SQL이 실행되고 commit() 시 실제 트랜잭션이 확정되어 DB에 반영된다. EntityManagerFactory는 싱글톤으로 관리되며 EntityManager는 작업 단위마다 생성된다.

-mapping 

@Entity
@Table(name = "tbl_menu")
public class Menu {

    @Id
    @Column(name = "menu_code")
    private int menuCode;

    @Column(name = "menu_name")
    private String menuName;

    @Column(name = "menu_price")
    private int menuPrice;
}


## 느낀점


## 활용하기
      
