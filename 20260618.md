
## 20260617 수업일지

## 오늘 배운것
오늘은 Spring Data JPA를 활용한 조회 기능 구현과 페이징 처리에 대해 학습하였다.

JpaRepository의 기본 메서드인 findById(), findAll()을 활용하여 메뉴 단건 조회 및 목록 조회를 구현하였다.
Optional과 orElseThrow()를 사용하여 조회 결과가 없을 경우 예외를 발생시키는 방법을 학습하였다.
Pageable과 Page를 이용하여 대용량 데이터를 효율적으로 조회하는 페이징 기능을 구현하였다.
@PageableDefault를 사용하여 기본 페이지 크기와 정렬 기준을 설정하는 방법을 익혔다.
Spring Data JPA의 쿼리 메서드 기능을 활용하여 findByMenuPriceLessThanOrderByMenuPriceDesc(), findByMenuPriceGreaterThan()와 같은 메서드를 작성하고 조건 검색을 구현하였다.
ModelMapper를 사용하여 Entity 객체를 DTO로 변환하는 방법을 학습하였다.
Page<Entity>를 Page<DTO>로 변환하기 위해 Page.map() 메서드를 활용하는 방법을 익혔다.
Controller, Service, Repository 계층을 분리하여 역할에 맞게 구현하는 계층형 구조를 학습하였다.
@RequestParam, @PathVariable을 사용하여 요청 데이터를 전달받는 방법을 복습하였다.
ResponseEntity를 활용하여 HTTP 응답을 구성하는 방법을 학습하였다.



## 느낀점
Spring Data JPA를 사용하니 직접 SQL을 작성하지 않고도 메서드 이름만으로 다양한 조회 기능을 구현할 수 있어 생산성이 높다는 것을 느꼈다. 특히 페이징 기능을 Pageable과 Page만으로 간단하게 구현할 수 있어 매우 편리하다고 생각했다. 또한 Entity를 DTO로 변환할 때 ModelMapper를 사용하면 반복적인 코드를 줄일 수 있어 코드의 가독성과 유지보수성이 향상된다는 점을 알게 되었다.

## 활용하기
  앞으로 REST API 개발 시 Spring Data JPA의 쿼리 메서드와 페이징 기능을 적극 활용하여 효율적인 데이터 조회 기능을 구현할 수 있을 것이다. 또한 DTO와 Entity를 분리하여 계층 간 의존성을 줄이고, ModelMapper를 활용하여 객체 변환 작업을 단순화함으로써 유지보수가 쉬운 애플리케이션을 개발하는 데 활용할 수 있을 것으로 생각한다.
