
## 20260528 수업일지

## 오늘 배운것
@GetMapping, @PostMapping, @DeleteMapping과 같은 매핑 어노테이션을 사용하여 HTTP 요청을 처리하는 방법을 실습하였다.
GET 방식은 데이터 조회, POST 방식은 데이터 저장, DELETE 방식은 데이터 삭제와 같이 HTTP 메서드에 따라 역할이 다르다는 점을 이해하였다.

@RequestParam을 사용하여 요청 파라미터를 받아오는 방법과 ResponseEntity를 사용하여 응답 데이터를 JSON 형태로 반환하는 방법도 학습하였다.
Map과 LinkedHashMap을 활용하여 응답 데이터를 구성하고, 클라이언트와 서버 간 데이터가 JSON 형식으로 전달되는 과정도 확인하였다.

또한 HttpSession 객체를 사용하여 세션 데이터를 저장, 조회, 삭제하는 기능도 구현해보았다.
세션을 이용하면 사용자별 데이터를 서버에 저장하여 로그인 상태 유지와 같은 기능을 구현할 수 있다는 점을 이해하였다.

이번 학습을 통해 Spring Boot에서 REST API를 구현하는 기본 흐름과 HTTP 요청 처리 방식을 이해할 수 있었으며, 웹 애플리케이션에서 클라이언트와 서버가 데이터를 주고받는 구조를 직접 실습

## 느낀점
@GetMapping, @PostMapping과 같은 어노테이션만으로 요청을 처리할 수 있어 기존 Servlet 방식보다 훨씬 간단하고 직관적이라고 느꼈다

## 활용하기
@GetMapping, @PostMapping과 같은 어노테이션만으로 요청을 처리할 수 있어 기존 Servlet 방식보다 훨씬 간단하고 직관적이라고 느꼈다
