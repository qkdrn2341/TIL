
## 20260501 수업일지

## 오늘 배운것

오늘은 이중 배열과 배열 정렬, 복사 개념, 클래스와 캡슐화에 대해 학습하였다.
이중 배열은 행과 열 구조로 데이터를 저장하며 중첩 반복문으로 접근할 수 있다.
Arrays.sort()를 통해 배열을 오름차순으로 정렬하는 방법을 익혔다.
얕은 복사는 주소를 공유하여 값이 함께 변경되는 특징이 있다.
깊은 복사는 값을 새로 복사하여 서로 영향을 주지 않는다.
클래스는 데이터와 기능을 하나로 묶는 객체지향의 기본 단위이다.
캡슐화는 필드 접근을 제한하고 메소드를 통해 제어하는 방식이다.
접근 제한자를 통해 클래스와 멤버의 접근 범위를 설정할 수 있다.
일반적으로 필드는 private으로 선언하고 getter/setter를 사용한다.
전체적으로 객체지향 개념의 중요성을 이해할 수 있었다.


## 느낀점
배열과 객체 개념이 점점 연결되면서 프로그래밍 구조를 이해하는 데 도움이 되었고, 특히 캡슐화를 통해 데이터를 보호하는 방식이 중요하다고 느꼈다.


## 활용하기
유지보수성을 증가시키기 위해 객체의 필드값은 private 으로 선언하고 getter/setter 를 이용해서 가져오고 수정하자
