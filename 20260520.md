
## 20260520 수업일지

## 오늘 배운것
오늘은 Servlet의 기본 동작 방식과 HTTP 요청 처리 방법에 대해 학습하였다.
GET 방식과 POST 방식의 차이를 이해하고, 각각의 요청을 처리하기 위해 doGet()과 doPost() 메소드를 사용하는 방법을 실습하였다. 또한 서블릿이 처음 생성될 때 실행되는 init() 메소드의 역할도 배웠다.

추가로 JSON 데이터를 자바 객체로 변환하거나, 자바 객체를 JSON 형태로 응답하기 위해 Jackson Databind 라이브러리의 ObjectMapper를 사용해보았다. serialize와 deserialize 개념을 학습하며 클라이언트와 서버 간 데이터 통신 과정을 이해할 수 있었다.

이번 학습을 통해 프론트엔드와 백엔드가 데이터를 주고받는 흐름을 이해할 수 있었다.
## 느낀점
Servlet의 생명주기와 init(), doGet(), doPost() 메소드의 역할을 직접 확인하면서 서버가 요청을 처리하는 흐름을 배울 수 있었다

## 활용하기
httpServlet 상속받아 init(), doGet(), doPost() 오버라이드 하여
get , post 요청에 대응하는 함수 작성
@WebServlet("") 어노테이션으로 실행할 서블릿매핑

