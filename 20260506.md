
## 20260506 수업일지

## 오늘 배운것

1. 객체 생성과 초기화 순서

객체를 new로 생성하면 다음 순서로 초기화가 진행된다.

static 초기화 블록
클래스가 메모리에 로드될 때 단 한 번만 실행된다.
static 필드 초기화나 복잡한 로직 처리에 사용한다.
명시적 초기값
필드를 선언하면서 바로 값을 넣는 방식이다.
객체를 생성할 때마다 실행된다.
인스턴스 초기화 블록
생성자보다 먼저 실행된다.
여러 생성자에서 공통으로 사용하는 초기화 코드를 모아둘 때 사용한다.
생성자
객체 생성의 마지막 단계이다.
전달받은 값을 기반으로 최종 초기화를 수행한다.

2. static

2.1static은 객체 생성 없이 클래스 이름으로 바로 접근 가능한 멤버를 의미한다.

특징
클래스가 메모리에 로드될 때 생성된다.
객체마다 공유된다.
객체 생성 전에 이미 존재한다.

2.2static 메소드 특징
객체 없이 호출 가능
this 사용 불가
인스턴스 변수 접근 불가

2.3static 초기화 블록

클래스 로드 시 딱 한 번 실행된다.

사용하는 이유
반복문
복잡한 계산
DB 조회
static 필드 초기화

같은 단순 대입 이상의 작업이 필요할 때 사용한다.

인스턴스 변수와 static 변수 차이
static 변수
클래스 소속
Method Area(static 영역)에 저장
객체 생성 전부터 존재
인스턴스 변수
객체 소속
객체 생성 시 Heap 영역에 저장

따라서 static 블록에서는 인스턴스 변수 접근이 불가능하다.

2.4public static final

공유하면서 절대 바뀌지 않는 상수를 만들 때 사용한다.

특징
static → 객체들이 공유
final → 값 변경 불가
네이밍 규칙
전부 대문자
단어는 _로 구분
SNAKE_CASE 사용

3. 디자인 패턴

자주 발생하는 문제를 효과적으로 해결하기 위한 검증된 설계 방식이다.

싱글턴(Singleton) 패턴

프로그램 전체에서 객체를 단 하나만 생성하도록 제한하는 패턴이다.

사용하는 이유
메모리 절약
객체 공유
공통 자원 관리
사용 예시
DB 연결 객체
로그 시스템
설정 관리 객체

싱글턴 구현 규칙
- 생성자를 private으로 막는다

외부에서 new 사용 금지

- Eager Singleton

객체를 미리 생성하는 방식

특징
구현 단순
속도 빠름
사용 안 해도 객체 생성됨
private static final Singleton instance = new Singleton();

- Lazy Singleton

필요할 때 객체를 생성하는 방식

특징
메모리 효율적
처음 호출 시 생성
if(instance == null) {
    instance = new Singleton();
}

## 느낀점
객체가 생성되는 과정과 메모리 구조를 순서대로 이해할 수 있었다. 객체의 생명주기와 클래스 동작 흐름을 함께 이해하면서 객체지향 구조를 학습했다.

## 활용하기
생성자를 private으로 막고 정적 메서드를 통해 하나의 객체만 반환하여 메모리 낭비를 방지하고 객체를 효율적으로 관리