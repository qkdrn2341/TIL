
## 20260521 수업일지

## 오늘 배운것
오늘은 Servlet 기반 REST API 구조와 React 연동 방식을 학습하였다.
클라이언트에서 fetch API를 사용해 요청을 보내면 Servlet이 요청을 받아 Service 계층으로 전달하고, Service는 DAO를 통해 DB에 접근하여 데이터를 조회하거나 저장하는 흐름을 실습하였다.
DAO에서는 SQL을 실행하고 ResultSet 데이터를 DTO 객체로 변환하였으며, Servlet에서는 Jackson ObjectMapper를 사용하여 DTO 객체를 JSON 형태로 직렬화하여 응답하였다.
또한 React와 Zustand를 이용해 서버 데이터를 상태로 관리하고, API 요청 결과를 화면에 렌더링하는 방법도 함께 학습하였다.

## 느낀점
아직 익숙하지 않아서 좀 어려웠다.

## 활용하기


