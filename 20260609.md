
## 20260609 수업일지

## 오늘 배운것
JDBC API 를 이용해 직접 SQL을 다룰 때 발생할 수 있는 문제인
코드 중복이나 , SQL 의존, 패러다임 불일치와 동일성 보장 문제등을 알게되었고
JPA(Java Persistence API)의 개념과 ORM(Object Relational Mapping)에 대해 학습하였다. Entity와 EntityManager, EntityManagerFactory의 역할을 이해하고, 영속성 컨텍스트(Persistence Context)를 통해 엔티티를 관리하는 방법을 실습하였다. 또한 find(), persist(), remove() 등의 CRUD 기능을 활용하여 데이터를 조회하고 관리하는 방법을 익혔으며, JUnit의 @ParameterizedTest를 사용하여 다양한 입력값에 대한 테스트를 수행해 보았다.



## 느낀점
JAP 를 사용하니 SQL 작성량이 크게 줄어들고 객체 중심으로 개발할 수 있었고 엔티티 객체를 통해 데이터를 조회하고 관리하는 방식이 직관적이어서 개발이 훨씬 편리하게 느껴졌다.

## 활용하기
        EntityManagerFactory factory = EntityManagerFactoryGenerator.getInstance(); EntityManagerFactory에서 EntityManager 인스턴스를 생성(반환)받는다.

         private static EntityManagerFactory factory = Persistence.createEntityManagerFactory("jpatest");
