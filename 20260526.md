
## 20260526 수업일지

## 오늘 배운것
오늘은 Spring Framework의 핵심 개념인 IoC Container와 Dependency Injection(DI)에 대해 학습하였다.
Spring Container는 객체를 생성하고 보관하며 관리하는 공간이고, 이때 스프링이 관리하는 객체를 Bean이라고 한다는 점을 이해하였다.

Bean을 등록하는 방식으로는 XML 설정 방식, Java Config에서 @Bean 어노테이션을 사용하는 방식, 그리고 @ComponentScan을 통해 @Component 계열 어노테이션을 자동 스캔하는 방식을 배웠다.

또한 실제 애플리케이션에서는 객체가 혼자 동작하지 않고 서로 다른 객체들을 필요로 한다는 점을 학습하였다.
예를 들어 Servlet은 Service를 호출하고, Service는 비즈니스 로직 처리를 위해 DAO를 호출하며, DAO는 JDBC 객체를 이용해 데이터베이스에 접근한다.
이처럼 다른 객체에 의존하는 관계를 의존성이라고 하며, 객체를 직접 생성하지 않고 외부에서 전달받는 방식을 의존성 주입(DI)이라고 한다는 것을 이해하였다.

의존성 주입 방식으로는 생성자 주입, Setter 주입, Field 주입 방식을 학습하였다.
생성자 주입은 객체 생성 시점에 필요한 의존 객체를 반드시 전달받기 때문에 필수 의존성에 적합하다는 점을 배웠고, Setter 주입과 Field 주입의 차이점도 함께 이해할 수 있었다.

## 느낀점
스프링이 객체 간의 관계를 어떻게 관리하고 조립하는지 흐름을 이해할 수있었다

## 활용하기
1. ComponentScan 이 클래스 탐색
2. Spring Container 가 Bean 생성
3. Controller 에 Service 주입
4. Service 에 DAO 주입
5. 객체들이 자동 연결됨
