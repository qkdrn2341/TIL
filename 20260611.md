
## 20260611 수업일지

## 오늘 배운것
오늘은 JPA의 연관관계 매핑(Association Mapping)에 대해 학습하였다.

객체 간의 관계를 데이터베이스 테이블 관계와 매핑하는 방법을 학습하였다.
단방향(Unidirectional)과 양방향(Bidirectional) 연관관계의 차이를 이해하였다.
@ManyToOne, @OneToMany, @OneToOne, @ManyToMany 어노테이션의 사용 방법을 학습하였다.
다대일(N:1) 관계에서 외래키(FK)를 가진 엔티티가 연관관계의 주인이 된다는 것을 이해하였다.
@JoinColumn을 사용하여 외래키 컬럼을 매핑하는 방법을 학습하였다.
양방향 관계에서 mappedBy 속성을 이용하여 연관관계의 주인이 아닌 엔티티를 설정하는 방법을 익혔다.
Menu와 Category 예제를 통해 N:1 관계를 구현하고 조회하는 실습을 진행하였다.
엔티티 객체를 저장할 때 연관된 엔티티 객체를 함께 설정하여 영속화하는 방법을 학습하였다.
JPQL에서 연관관계를 이용한 객체 중심 조회 방식을 확인하였다.
연관관계 매핑 시 무분별한 양방향 관계 사용보다 필요한 방향만 설정하는 것이 중요함을 이해하였다.


## 느낀점

기존에는 데이터베이스의 외래키를 중심으로 관계를 생각했지만, JPA에서는 객체 간 참조를 통해 관계를 표현한다는 점이 인상적이었다. 특히 @ManyToOne 관계가 가장 많이 사용된다는 점과 연관관계의 주인 개념이 중요하다는 것을 알게 되었다. 처음에는 mappedBy와 연관관계 주인의 개념이 다소 헷갈렸지만, 실제 예제를 통해 객체 그래프를 탐색하는 방식이 SQL보다 직관적이라는 것을 느꼈다.
## 활용하기
      
관계형 데이터를 설계할 때 JPA의 연관관계 매핑을 활용할 수 있다