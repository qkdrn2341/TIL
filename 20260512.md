
## 20260512 수업일지

## 오늘 배운것

스트림(Stream)은 데이터가 이동하는 통로이며, 자바에서는 파일을 읽거나 저장할 때 스트림을 사용한다는 것을 배웠다.

파일 스트림은 크게 바이트 스트림(Byte Stream)과 문자 스트림(Character Stream)으로 나뉜다.

바이트 스트림은 1바이트 단위로 데이터를 처리하며 이미지, 동영상 등 모든 파일을 다룰 수 있다. 대표적으로 FileInputStream, FileOutputStream 클래스를 사용하였다.

문자 스트림은 문자 단위로 데이터를 처리하며 텍스트 파일을 다룰 때 사용한다. 한글 처리에 더 적합하며 FileReader, FileWriter 클래스로 사용
Input은 데이터를 읽는 기능이고 Output은 데이터를 저장하는 기능.

추가로 BufferedReader, BufferedWriter 같은 보조 스트림을 사용하면 입출력 속도를 향상시킨다




## 느낀점
파일 입출력의 기본 구조와 바이트 스트림, 문자 스트림의 차이점을 이해할 수 있었다.

## 활용하기
이미지나 파일을 읽어올때 사용