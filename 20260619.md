
## 20260619 수업일지

## 오늘 배운것
1. Outbox Pattern

Outbox Pattern은 데이터베이스 변경과 메시지 발행을 안전하게 처리하기 위한 패턴이다.

일반적으로 주문 생성 후 Kafka와 같은 메시지 브로커에 이벤트를 발행해야 하는데, DB 저장은 성공했지만 메시지 발행이 실패하면 데이터 불일치가 발생할 수 있다.

이를 해결하기 위해 비즈니스 데이터와 이벤트 데이터를 같은 트랜잭션으로 저장한다.

주문 생성
    ↓
orders 저장
    ↓
outbox 저장
    ↓ (동일 트랜잭션)
commit
    ↓
이벤트 발행 프로세스가 outbox 읽음
    ↓
Kafka 발행

2. 멱등성(Idempotency)

멱등성이란 같은 요청을 여러 번 수행해도 결과가 동일하게 유지되는 성질이다.

3. BFF (Backend For Frontend)

BFF는 프론트엔드별로 전용 백엔드를 두는 아키텍처 패턴이다.

기존 구조

Web
Mobile
   ↓
API Gateway
   ↓
User Service
Order Service
Product Service

문제점

모바일과 웹이 원하는 데이터가 다름
불필요한 API 호출 증가
프론트엔드 로직 복잡

BFF 적용

Web
 ↓
Web BFF
 ↓
서비스들

Mobile
 ↓
Mobile BFF
 ↓
서비스들

## 느낀점
Outbox Pattern은 MSA 환경에서 데이터 정합성을 유지하기 위한 핵심 기술이라는 것을 알게 되었다. 멱등성은 결제나 주문과 같이 중복 실행이 치명적인 기능에서 반드시 고려해야 하는 요소임을 이해하였다. 또한 BFF를 통해 프론트엔드와 백엔드의 역할을 명확히 분리하고 사용자 환경에 맞는 API를 제공할 수 있다는 점이 인상적이었다.

## 활용하기
주식 모의투자 프로젝트나 알림 시스템을 구축할 때 Outbox Pattern을 적용하여 이벤트 유실을 방지할 수 있다. 주문 생성이나 포인트 지급 기능에는 멱등성을 적용하여 중복 처리를 방지할 수 있으며, 웹과 앱을 동시에 지원하는 서비스에서는 BFF를 도입하여 각 클라이언트에 최적화된 API를 제공할 수 있을 것으로 생각한다
