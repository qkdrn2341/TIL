
## 20260422 수업일지

## 오늘 배운것

Next.js : REACT.JS의  CSR(Client Side Rendering) 렌더링 단점인 첫 페이지가 화면에 렌더링이 되기까지의 걸리는 시간을 개선한 Framwork
server-side rendering을 통해 html을 서버에서 만들어져오기 때문에 페이지 로딩속도가 빠르다.

- router
router 는 페이지 이동(라우팅)을 담당하는 기능
폴더 기반 자동 라우팅은 app/ 디렉토리 내의 파일과 폴더 구조를 통해 라우팅을 정의한다. 폴더이름이 곧 url의 경로가 된다.

- page.tsx : 해당 경로의 페이지를 정의
- layout.tsx : 는 해당 경로와 하위 경로에 적용되는 레이아웃을 정의

라우팅에 사용되는 페이지 컴포넌트를 export 하여 다른 페이지에서 import 해서 사용한다.



## 느낀점
리액트는 자유도가 높지만 기본적인 기능이외에는 직접 만들어서 사용해야 한다는 불편함이 있었는데 Next.js는 큰 틀 안에서 개발하는 과정에서 자유도는 떨어지지만 제공하는 기능이 많아 개발자가 개발하기에 편리하다고 느껴졌다.


## 활용하기
폴더의 구조를 통해 페이지를 나누고, 필요한 데이터는 파일이나 API에서 가져와 화면에 출력한다. public 폴더는 이미지나 정적 파일을 저장하는 용도로 사용한다.