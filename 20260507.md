
## 20260507 수업일지

## 오늘 배운것

오늘은 자바의 객체지향 핵심 개념인 다형성, 추상 클래스, 인터페이스에 대해 학습하였다.

다형성은 부모 타입 변수에 자식 객체를 담아 다양한 객체를 하나의 타입으로 제어하는 기술이다.
Animal uCat = new Cat(); 과 같이 업캐스팅을 통해 자식 객체를 부모 타입으로 다룰 수 있다.
실행 시 실제 객체의 오버라이딩 메서드가 호출되는 것을 동적 바인딩이라고 한다.
부모 타입으로 변환된 객체를 다시 자식 타입으로 사용하는 것은 다운캐스팅이다.
추상 클래스는 미완성 설계도로 직접 객체 생성이 불가능하며 공통 기능과 규칙을 정의한다.
추상 메서드는 구현부가 없는 메서드이며 자식 클래스가 반드시 오버라이딩해야 한다.
인터페이스는 객체 간의 공통 규격(표준)을 정의하는 역할을 한다.
인터페이스 변수는 자동으로 public static final 상수가 된다.
인터페이스의 메서드는 기본적으로 public abstract가 적용된다.
default 메서드를 통해 기존 구현체 수정 없이 기능을 추가할 수 있으며 이는 하위 호환성을 위한 기능이다.
implements 키워드를 사용하여 인터페이스를 구현하며 여러 인터페이스를 동시에 구현할 수 있다.
인터페이스 타입을 사용하는 것은 구체적인 클래스에 의존하지 않는 느슨한 결합 구조를 만들기 위함이다.
추상 클래스는 공통 기능을 강제해야 하는 게임 캐릭터, 플레이어 시스템 등에 활용 가능하다.

=>인터페이스는 키보드, 마우스, 네트워크 장치처럼 다양한 구현체를 동일한 방식으로 제어할 때 사용된다.
느슨한 결합 구조를 통해 새로운 기능 추가 시 기존 코드를 크게 수정하지 않아도 된다.
객체지향 설계를 통해 확장성과 재사용성이 높은 프로그램을 만들 수 있다.

## 느낀점
단일 상속만 가능한 추상 클래스와 여러개를 구현 가능한 인터페이스의 차이점을 잘 알고 활용하는 것이 중요하다고 느꼈다

## 활용하기
다형성을 활용하면 여러 객체를 하나의 부모 타입으로 관리할 수 있어 유지보수가 쉬워진다.