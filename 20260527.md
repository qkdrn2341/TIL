
## 20260527 수업일지

## 오늘 배운것
singleton scope는 컨테이너 안에서 객체를 하나만 생성하여 공유하는 방식이며, prototype scope는 Bean을 요청할 때마다 새로운 객체를 생성하는 방식
Bean Scope를 활용하면 singleton 방식으로 공통 객체를 효율적으로 공유하여 메모리 사용을 줄일 수 있고, prototype 방식으로는 요청마다 새로운 객체를 생성하여 독립적인 작업 처리가 가능
생성 → 의존성 주입 → 초기화 → 사용 → 소멸
변경 가능성이 있는 값을 Java 코드에 직접 작성하지 않고 properties 파일에 저장한 뒤 @PropertySource와 @Value 어노테이션을 사용하여 값을 읽어올수있다.

AOP는 공통적으로 사용되는 기능을 핵심 로직과 분리하여 관리하는 방식이며, 로그 출력이나 실행 시간 측정, 트랜잭션 처리와 같은 반복적인 기능을 효율적으로 관리할 수 있다


## 느낀점
스프링이 객체 간의 관계를 어떻게 관리하고 조립하는지 흐름을 이해할 수있었다

## 활용하기
properties 파일을 통한 외부 설정 관리 기능은 데이터베이스 정보, API 키, 서버 주소와 같이 자주 변경될 수 있는 설정값을 코드와 분리하여 관리할 수 있으므로 유지보수성과 보안성을 높일 수 있다.
