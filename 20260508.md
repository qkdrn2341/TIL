
## 20260508 수업일지

## 오늘 배운것

오늘은 자바에서 자주 사용되는 API와 객체 관련 핵심 개념들을 학습하였다.
Wrapper 클래스의 박싱(Boxing)과 언박싱(Unboxing)을 통해 기본자료형과 객체형 간의 변환 방법을 이해하였다.
오토 박싱과 오토 언박싱 기능을 사용하면 자동으로 형 변환이 이루어진다는 것도 확인하였다.
또한 DateTime API를 활용하여 현재 날짜와 시간 정보를 가져오고 원하는 형식으로 출력하는 방법을 학습하였다.
객체의 정보를 문자열 형태로 출력하기 위해 toString() 메소드를 오버라이딩하는 방법도 배웠다.
객체 비교 시 주소값이 아닌 실제 값을 비교하기 위해 equals() 메소드를 재정의하는 방법을 실습하였다.
추가로 객체의 고유 해시값을 생성하는 hashCode() 메소드의 역할과 사용 이유를 이해하였다.
특히 equals()와 hashCode()는 함께 오버라이딩해야 한다는 점을 학습하였다.
이번 학습을 통해 객체 지향 프로그래밍에서 객체를 효율적으로 관리하고 비교하는 방법을 익힐 수 있었다.

## 느낀점
자바에 다양한 api들을 활용하면 좋을거 같다

## 활용하기
equals()와 hashCode()를 활용하면 객체 비교 및 컬렉션(Set, Map) 관리에 효율적으로 사용할 수 있고
박싱과 언박싱 기능은 컬렉션 프레임워크 사용 시 기본 자료형 데이터를 객체 형태로 저장할 때 활용할 수 있다.