
## 20260529 수업일지

## 오늘 배운것
Spring Boot에서 REST API를 구현하며 JSON 데이터 처리 방식을 학습하였다.
GET 요청을 통해 List<MemberDTO> 객체를 JSON 배열 형태로 반환하는 방법을 실습하였다.
Path Variable을 사용하여 특정 회원 번호(memberNo)로 데이터를 조회하는 기능을 구현하였다.
POST 요청에서 JSON 요청 본문(Request Body)을 MemberDTO 객체로 자동 변환하는 방법을 학습하였다.
응답 데이터를 message와 data 구조로 감싸는 Response Wrapper 패턴을 적용해보았다.
ObjectMapper를 활용하여 Java 객체를 JSON으로 변환(Serialize)하고 JSON을 Java 객체로 변환(Deserialize)하는 과정을 실습하였다.
RESTful API의 요청 방식(GET, POST)과 데이터 전달 구조를 이해하였다.
HTTP 요청 및 응답에서 Content-Type의 역할을 확인하였다.
DTO를 활용하여 데이터를 전달하는 구조를 학습하였다.
Spring Boot에서 JSON 기반 API를 구현하는 전반적인 흐름을 익혔다.

## 느낀점
Spring Boot가 객체와 JSON 간의 변환을 자동으로 처리해준다는 점이 인상적이었다

## 활용하기
ObjectMapper와 DTO를 이용하여 다양한 형태의 데이터를 효율적으로 처리
