
## 20260615 수업일지

## 오늘 배운것
Native Query는 JPQL이 아닌 실제 데이터베이스 SQL을 직접 작성하여 실행하는 기능임을 이해하였다.
JPQL은 엔티티와 필드명을 사용하지만, Native Query는 실제 테이블명과 컬럼명을 사용한다는 차이를 학습하였다.
EntityManager#createNativeQuery()를 이용하여 SQL을 실행하는 방법을 익혔다.
데이터베이스에서 제공하는 함수나 복잡한 SQL을 JPA에서도 그대로 사용할 수 있다는 점을 확인하였다.
Native Query 결과를 엔티티 객체 또는 Object 배열 형태로 매핑하는 방법을 학습하였다.
JPQL로 표현하기 어려운 복잡한 집계, 서브쿼리, DB 전용 함수 등을 사용할 때 Native Query가 유용함을 이해하였다.
Spring Data JPA에서는 @Query(nativeQuery = true) 옵션을 사용하여 Native Query를 작성할 수 있음을 학습하였다.

PQL과 Native Query 차이
구분	JPQL	Native Query
조회 대상	엔티티 객체	실제 테이블
사용 이름	엔티티명, 필드명	테이블명, 컬럼명
DB 독립성	높음	낮음
복잡한 SQL	제한적	자유롭게 사용
DB 함수 사용	일부 제한	가능


## 느낀점
PQL은 객체지향적으로 데이터를 조회할 수 있어 편리했지만, 데이터베이스 고유 기능이나 복잡한 SQL을 사용할 때는 한계가 있다는 것을 알게 되었다. Native Query를 사용하면 기존 SQL 문법을 그대로 활용할 수 있어 유연성이 높다는 장점이 있었지만, 특정 DB에 의존하게 되어 이식성이 떨어질 수 있다는 점도 이해하였다

## 활용하기
일반적인 CRUD와 단순 조회는 JPQL 또는 Spring Data JPA의 쿼리 메서드를 사용하고, 복잡한 통계 조회나 데이터베이스 함수 사용이 필요한 경우에는 Native Query를 활용할 수 있을 것이다. 또한 성능 최적화가 필요한 특정 조회 기능에서 Native Query를 적용하여 효율적인 데이터 처리를 구현할 수 있을 것으로 생각한다.
