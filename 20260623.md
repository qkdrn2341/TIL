
## 20260623 수업일지

## 오늘 배운것
프록시(Proxy)
식당에서 주문할 때 주방에 직접 가지 않고 직원에게 주문하고
직원이 중간에서 대신 전달해주는 것처럼, 프록시도 중간에서 요청을 받아 대신 처리


Spring AOP 구조
클라이언트
    ↓
프록시 객체
    ↓
실제 객체(Target)

Controller
    ↓
UserService Proxy
    ↓
UserService

프록시가:

로그 찍고
트랜잭션 시작하고
권한 검사하고
메서드 실행시키고
트랜잭션 종료

프록시 = 실제 객체 앞에서 요청을 대신 받아 추가 작업을 수행한 뒤 실제 객체에게 전달하는 대리 객체.

## 느낀점
스프링의 @Transactional, @AOP, @Cacheable, @Async 등이 모두 프록시 기술을 사용

## 활용하기
AOP는 프록시 기반으로 동작한다
