
## 20260604 수업일지

## 오늘 배운것
#driver config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/menudb
    username: ohgiraffers
    password: ohgiraffers

#mybatis config
mybatis:
  mapper-locations: mappers/**/*.xml // MyBatis가 SQL이 작성된 Mapper XML 파일을 어디서 찾을지 지정

.yml 으로 db와 mybatis mapper-locations

https 요청 -> Controller -> Service -> Mapper 순으로 메소드를 작성해보았다.
서비스와 컨트롤러를 컨테이너에서 빈으로 등록하여 생성자 주입으로 의존성을 주입받아 객체 간의 결합도를 낮추고 유지보수성을 향상시켰다.


## 느낀점
Spring MyBatis를 활용한 CRUD 구현을 통해 Spring MyBatis의 전체적인 구조와 데이터 처리 흐름을 이해하게 되었다

## 활용하기
트랜잭션 처리와 SqlSession 관리 방식을 이해한 만큼 안정적인 CRUD 기능 개발에 활용
