
## 20260429 수업일지

## 오늘 배운것

메소드는 특정 작업을 수행하는 코드 집합이며, JVM은 프로그램 시작 시 main()을 먼저 실행한다.
메소드는 main() 밖에서 정의하고, main() 안에서 호출하여 실행한다.
일반 메소드는 객체를 생성한 후 객체.메소드() 형태로 호출한다.
객체 생성은 new 키워드를 사용하며, 객체는 Heap 영역에 생성되고 참조 변수는 Stack에 저장된다.
이 과정을 인스턴스화라고 하며, 객체를 통해 인스턴스 메소드를 호출할 수 있다.
static 메소드는 클래스에 속하며 객체 생성 없이 클래스명.메소드()로 호출 가능하다.
같은 클래스 내부에서는 클래스명을 생략할 수 있다.
메소드 호출 시 전달하는 값은 argument, 이를 받는 변수는 parameter이다.
다른 클래스의 메소드는 import 후 사용하거나, 패키지명을 포함해 호출한다.
Math 클래스는 java.lang에 포함된 static 메소드 집합으로 import 없이 사용 가능하다.

## 느낀점
IDE 를 사용해서 쓰니 컴파일 오류도 나서 빌드해보지 않고도 확인이 가능해서 너무 좋다.


## 활용하기
메서드를 사용할때는 객체를 인스턴스화 해서 써야 하고 , 정적 메서드일때는 클래스명과 함께 써주자.
Math.random 을 사용해 난수 표현